<!doctype html>
<html class="no-js" lang="{{ request.locale.iso_code }}">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="theme-color" content="{{ settings.color_background }}">
    <link rel="canonical" href="{{ canonical_url }}">
    <link rel="preconnect" href="https://cdn.shopify.com" crossorigin>

    {%- if settings.favicon != blank -%}
      <link rel="icon" type="image/png" href="{{ settings.favicon | image_url: width: 32, height: 32 }}">
    {%- endif -%}

    <title>
      {{ page_title }}
      {%- if current_tags %} &ndash; tagged "{{ current_tags | join: ', ' }}"{% endif -%}
      {%- if current_page != 1 %} &ndash; Page {{ current_page }}{% endif -%}
      {%- unless page_title contains shop.name %} &ndash; {{ shop.name }}{% endunless -%}
    </title>

    {% if page_description %}
      <meta name="description" content="{{ page_description | escape }}">
    {% endif %}

    {% render 'seo-meta-tags' %}

    <style>
      {{ settings.type_body_font | font_face: font_display: 'swap' }}
      {{ settings.type_body_font | font_modify: 'weight', 'bold' | font_face: font_display: 'swap' }}
      {{ settings.type_heading_font | font_face: font_display: 'swap' }}
      {{ settings.type_heading_font | font_modify: 'weight', 'bold' | font_face: font_display: 'swap' }}

      :root {
        --color-primary: {{ settings.color_heading }};
        --color-secondary: #ffffff;
        --color-accent: {{ settings.color_accent }};
        --color-text: {{ settings.color_foreground }};
        --color-background: {{ settings.color_background }};
        --font-body: {{ settings.type_body_font.family }}, {{ settings.type_body_font.fallback_families }};
        --font-heading: {{ settings.type_heading_font.family }}, {{ settings.type_heading_font.fallback_families }};
      }
    </style>

    {{ content_for_header }}

    {{ 'theme.css' | asset_url | stylesheet_tag }}
    <script src="{{ 'theme-interactions.js' | asset_url }}" defer></script>
    <script src="{{ 'recipient-form.js' | asset_url }}" defer></script>
    <script>
      window.accessibilityStrings = {
        recipientFormExpanded: {{ 'accessibility.recipient_form.expanded' | t | json }},
        recipientFormCollapsed: {{ 'accessibility.recipient_form.collapsed' | t | json }},
        predictiveSearchResults: {{ 'accessibility.predictive_search.results' | t | json }},
        predictiveSearchNoResults: {{ 'accessibility.predictive_search.no_results' | t | json }}
      };
      window.kanadAIConfig = {
        enabled: {{ settings.enable_ai_chatbot | json }},
        assistantName: {{ settings.ai_assistant_label | json }},
        welcomeMessage: {{ settings.ai_chat_welcome | json }},
        gatewayUrl: {{ settings.ai_gateway_url | json }},
        provider: {{ settings.ai_provider | json }},
        model: {% case settings.ai_provider %}{% when 'anthropic' %}{{ settings.ai_anthropic_model | default: settings.ai_model | json }}{% when 'google' %}{{ settings.ai_google_model | default: settings.ai_model | json }}{% when 'groq' %}{{ settings.ai_groq_model | default: settings.ai_model | json }}{% else %}{{ settings.ai_openai_model | default: settings.ai_model | json }}{% endcase %},
        systemPrompt: {{ settings.ai_system_prompt | strip_newlines | json }},
        voiceInputEnabled: {{ settings.enable_ai_voice_input | json }},
        voiceOutputEnabled: {{ settings.enable_ai_voice_output | json }},
        voiceLocale: {{ settings.ai_voice_locale | json }},
        voiceName: {{ settings.ai_voice_name | json }},
        pageContext: {
          template: {{ template.name | json }},
          pageTitle: {{ page_title | json }},
          canonicalUrl: {{ canonical_url | json }},
          shopName: {{ shop.name | json }},
          shopDomain: {{ shop.permanent_domain | json }},
          product: {% if product %}{
            id: {{ product.id | json }},
            title: {{ product.title | json }},
            handle: {{ product.handle | json }},
            vendor: {{ product.vendor | json }},
            type: {{ product.type | json }},
            tags: {{ product.tags | json }},
            priceMin: {{ product.price_min | json }},
            priceMax: {{ product.price_max | json }}
          }{% else %}null{% endif %},
          collection: {% if collection %}{
            title: {{ collection.title | json }},
            handle: {{ collection.handle | json }}
          }{% else %}null{% endif %},
          article: {% if article %}{
            title: {{ article.title | json }},
            author: {{ article.author | json }}
          }{% else %}null{% endif %},
          page: {% if page %}{
            title: {{ page.title | json }},
            handle: {{ page.handle | json }}
          }{% else %}null{% endif %}
        }
      };
    </script>
  </head>

  <body class="template-{{ template.name | handle }}">
    <a class="skip-to-content-link button visually-hidden" href="#MainContent">
      Skip to content
    </a>

    {%- unless template.name == 'password' -%}
      {% sections 'header-group' %}
    {%- endunless -%}

    <main id="MainContent" class="content-for-layout focus-none" role="main" tabindex="-1">
      {{ content_for_layout }}
    </main>

    {%- unless template.name == 'password' -%}
      {% sections 'footer-group' %}
    {%- endunless -%}

    {%- if settings.enable_ai_chatbot -%}
      {% render 'ai-concierge' %}
    {%- endif -%}
  </body>
</html>
